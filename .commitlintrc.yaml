extends:
  - "@commitlint/config-conventional"

rules:
  type-enum:
    - 2
    - always
    - [feat, fix, docs, style, refactor, test, chore, ci]
  scope-enum:
    - 2
    - always
    - [core, brain, eye, hand, memory, ui, api, config]
