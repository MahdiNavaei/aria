name: Documentation

on:
  push:
    branches: [main]
    paths:
      - 'Docs/**'

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Docs placeholder
        run: echo "Docs build not configured."
